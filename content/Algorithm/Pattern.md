### - 완전검색 Brutal force [[Exhaustive]]

```python
def BruteForce(p, t):
    # N: 본문(t)의 전체 길이, M: 찾으려는 패턴(p)의 길이
    N = len(t)
    M = len(p)
    
    i = 0 # t(본문)를 훑는 인덱스
    j = 0 # p(패턴)를 훑는 인덱스

    # 본문 끝에 도달하거나, 패턴을 완전히 찾을 때까지 반복
    while j < M and i < N:
        
        # [불일치 발생 시]
        if t[i] != p[j]:
            # i를 이번 비교를 시작했던 위치의 '다음 칸'으로 되돌림
            # i - j는 비교 시작점이고, 아래에서 i + 1을 해주므로 결과적으로 시작점+1이 됨
            # 본문 인덱스 - 패턴 인덱스니까 비교가 딱 시작한 곳
            i = i - j 
            
            # j는 패턴의 맨 처음으로 되돌림 (-1로 세팅 후 아래에서 +1 하면 0이 됨)
            j = -1
        
        # [공통 실행]
        # 1. 일치하면: 다음 글자를 비교하기 위해 둘 다 전진
        # 2. 불일치하면: 위에서 세팅한 위치(i-j, -1)에서 1씩 더해져 다음 시도 시작

        i = i + 1
        j = j + 1

    # [결과 반환]
    if j == M: 
        # j가 패턴 길이(M)와 같아졌다면 모든 글자가 일치했다는 뜻
        # i는 마지막 일치 후 +1이 된 상태이므로, i - M을 하면 패턴의 시작 인덱스가 나옴
        return i - M # 검색 성공
    else:
        return -1 # 본문 끝까지 갔으나 검색 실패
```


***
### - KMP
```python
def KMP_search(t, p):
    # ---------------------------------------------------------
    # 1. pi 테이블(LPS)을 만드는 내부 함수 (패턴 분석 단계)
    # ---------------------------------------------------------
    def get_pi(pattern):
        m = len(pattern)
        pi_table = [0] * m
        j = 0 # 접두사(Prefix) 인덱스
        
        for i in range(1, m): # i는 접미사(Suffix) 인덱스
            # 글자가 다르면, 이전에 일치했던 위치(pi_table[j-1])로 j를 되돌림
            while j > 0 and pattern[i] != pattern[j]:
                j = pi_table[j - 1]
            
            # 글자가 같으면, 일치하는 길이를 증가시키고 기록
            if pattern[i] == pattern[j]:
                j += 1
                pi_table[i] = j
        return pi_table

    # ---------------------------------------------------------
    # 2. 본문에서 패턴을 찾는 실제 로직 (검색 단계)
    # ---------------------------------------------------------
    n = len(t)
    m = len(p)
    pi = get_pi(p) # 위에서 만든 함수로 pi 테이블을 먼저 생성
    
    j = 0 # 패턴의 인덱스
    
    # i(본문 인덱스)는 0부터 끝까지 전진만 함 (되돌아가지 않음!)
    for i in range(n):
        # 현재 본문 글자와 패턴 글자가 다르면 j를 점프시킴
        while j > 0 and t[i] != p[j]:
            j = pi[j - 1] # pi 테이블을 보고 "중복되는 앞부분"으로 이동
            
        # 글자가 같으면
        if t[i] == p[j]:
            # 패턴의 끝까지 다 확인했다면 (검색 성공)
            if j == m - 1:
                return i - m + 1 # 패턴이 시작된 인덱스 반환
            else:
                j += 1 # 다음 글자 확인을 위해 패턴 인덱스 증가
                
    return -1 # 끝까지 다 봤는데 못 찾은 경우

# --- 사용 예시 ---
text = "ABC ABCDAB ABCDABCDABDE"
pattern = "ABCDABD"

result = KMP_search(text, pattern)
if result != -1:
    print(f"패턴이 인덱스 {result}에서 시작됩니다.")
else:
    print("패턴을 찾지 못했습니다.")
```



```plaintext
[1단계: 불일치 발생]
본문(t):    0   1   2   3   4   5   6
           A   B   A   B   A  |B| ...  <-- i=5에서 'B'
패턴(p):    A   B   A   B   A  |C|       <-- j=5에서 'C' (불일치!)
           0   1   2   3   4   5
           └───── 맞춘 부분 ─────┘
            (ABABA까지는 일치함)

[2단계: 패턴 내부의 '반복 마디' 확인]
맞춘 문자열(ABABA)을 쪼개보면:
           [A B A] B [A B A]
           접두사     접미사  <-- 이 두 마디가 길이 3으로 일치함!
           (그래서 pi[4] = 3)

[3단계: 마법의 점프 (j = pi[4] = 3)]
본문(i)은 5에 고정! 패턴(j)만 3으로 바꿉니다.

본문(t):    0   1   2   3   4   5   6
           A   B  (A   B   A) |B| ...  <-- i=5 (고정)
패턴(p):           (A   B   A) |B|  A   C
                    0   1   2  [3]  4   5
                                ^
           └──── 이미 일치 ────┘  j=3부터 다시 비교 시작!
             (재검사 생략구간)







인덱스: 0 1 2 3 4 5
패턴:  A B A B A C
pi값:  0 0 1 2 3 0

[불일치 발생!] 
j=5(C)에서 틀림. "직전까지 맞춘 5글자(ABABA)의 pi값은?" -> pi[4] = 3.

[점프!]
j를 바로 3으로 바꿉니다.

왜 3인가요?
- 0번 글자 (A) : 이미 맞음 (Skip)
- 1번 글자 (B) : 이미 맞음 (Skip)
- 2번 글자 (A) : 이미 맞음 (Skip)
- 3번 글자 (B) : 여기서부터 본문과 비교 시작! <-- 이 '3'이 바로 pi값입니다.
  ```

### 2. 얼마만큼 'Shift(이동)' 하는 건가요?

사용자님께서 "5개 일치했는데 왜 3칸만 갔지?"라고 하신 부분에서 아주 중요한 포인트를 짚으셨습니다. 여기서 **'이동 거리'**와 **'새로운 인덱스'**를 구분해야 합니다.

- **일치한 개수**: 5개 (`ABABA`)
- **재사용 가능한 길이(pi값)**: 3 (`ABA`)
- **실제 패턴이 밀리는 칸 수 (Shift)**: `5 - 3 = 2칸`

즉, **많이 겹칠수록(pi값이 클수록), 패턴은 조금만 밀립니다.** (이미 많이 맞춰놨으니까 조금만 옆으로 가서 나머지를 맞추려는 전략이죠!)

***
### - 보어스 무어


1. 나쁜 문자 이동 (Bad Character Heuristic)

비교하다가 본문과 패턴의 문자가 다를 때, 그 본문의 문자를 **'나쁜 문자'**라고 부릅니다. 이때 이동 거리는 패턴 내부에 그 문자가 어디에 위치하느냐에 따라 결정됩니다.

#### 핵심 규칙: "가장 오른쪽에 있는 문자에 맞춘다"

패턴 안에 중복 문자가 있다면, **불일치가 일어난 지점보다 왼쪽에 있으면서, 가장 오른쪽에 위치한 동일 문자**에 본문의 문자를 맞춥니다.

#### 예시: 패턴이 `ANANAS`일 때 (A와 N이 중복됨)

본문에서 불일치가 일어난 문자가 **'A'**라고 가정해 봅시다.

1. **패턴**: `A(0) N(1) A(2) N(3) A(4) S(5)` (인덱스)
    
2. 만약 `S(5)` 자리에서 본문의 `A`와 부딪혔다면?
    
3. 패턴에는 `A`가 0, 2, 4번에 있습니다.
    
4. 이때 가장 오른쪽에 있는 `A`(인덱스 4)를 본문의 `A` 위치와 겹치도록 **1칸만** 밉니다.
    

> **왜 가장 오른쪽인가요?** 너무 많이 밀어버리면 그 사이에 있을지도 모르는 정답을 건너뛸 수 있기 때문입니다. 가장 보수적으로(조금씩) 밀어서 안전하게 찾는 것이죠.

