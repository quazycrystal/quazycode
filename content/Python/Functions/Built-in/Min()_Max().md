### 1. 작동 원리: 선착순 

ex) numbers = `[10, 20, 5, 40, 15]
max_value = max(numbers)
min_value = min(numbers)

---

### 2. 우리가 원하는 것: "동점이면 인덱스 큰 놈이 이겨라"

이걸 구현하려면 심판의 **채점 기준표**를 바꿔야 합니다. 단순히 '값'만 보는 게 아니라 **'값 + 인덱스'**를 묶어서 세트로 보게 만들어야 하죠.

여기서 파이썬의 **"튜플 비교(Tuple Comparison) 법칙"**이 등장합니다. 이것만 이해하면 끝납니다.

    

이 원리를 이용해서 코드를 한번 짜보시겠어요?


곱하기 2T 이런식 표현 안 되고 2 * T 이런식으로 해야 함

max 튜플 들어있는 리스트도 가능! ex) `[ (1,2), (3,5) ]