Python에서 `if`문과 `break`를 함께 사용하는 것은 **"특정 조건이 충족되었을 때 반복(Loop)을 즉시 중단하라"**는 의미로 쓰입니다.

가장 중요한 점은 **`break`는 `if`문을 멈추는 것이 아니라, `if`문을 감싸고 있는 반복문(`for` 또는 `while`)을 멈춘다는 것**입니다.

-> if를 끝낼땐 브레이크 x. 그리고 이미 temp > k 조건에서 temp = 0이라고 바꿔줬으니까 알아서 나갈 듯